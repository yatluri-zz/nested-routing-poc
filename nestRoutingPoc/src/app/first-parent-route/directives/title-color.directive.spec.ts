import { TitleColorDirective } from './title-color.directive';

describe('TitleColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleColorDirective();
    expect(directive).toBeTruthy();
  });
});
